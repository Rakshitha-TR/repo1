name: My Job

on:
  push:
  workflow_dispatch:

jobs:
  first_job:
    runs-on: self-hosted
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: List and Read Files
        run: |
          echo "My first Github Actions Job"
          ls -ltra
