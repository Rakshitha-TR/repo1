name: My First workflow
on: push

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      -name actions/checkout@v4
        run: welcome message
      -name: List files
        run: ls
      -name: Make script executable
        run: chmod x heelo.sh
      -name: Execute shell script
        run: ./hello.sh